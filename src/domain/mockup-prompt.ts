// src/domain/mockup-prompt.ts
export function buildMockupPrompt(productDoc: string): string {
  return [
    '너는 이 프로젝트에서 *실제로 구현된* 앱 UI를 픽셀 단위로 똑같이 재현한 디자인 목업을 만든다.',
    '',
    '먼저 이 프로젝트의 실제 프런트엔드 UI 소스를 직접 읽어라 — 컴포넌트, 스타일/CSS, 라우팅(예: src/web 등). 추측하지 말고 *실제 코드*를 근거로 한다.',
    '',
    '그다음 단일 자체완결형 HTML 문서를 출력한다:',
    '1) 실제 화면들을 "아트보드"(카드)로 캔버스에 가로로 펼치고, 각 아트보드 위에 화면 이름 라벨을 단다.',
    '2) 레이아웃·컴포넌트·간격·색·타이포·문구가 실제 구현과 100% 일치해야 한다. 대충 비슷한 수준이 아니라 똑같이.',
    '3) 모든 상태와 인터랙티브/오버레이 요소를 빠짐없이 별도 아트보드로 포함한다: 모달·다이얼로그, 드롭다운·메뉴 열림, hover/focus, 빈 상태, 로딩, 오류, 토스트 등.',
    '4) 실제 데이터 대신 그럴듯한 가상 데이터로 채운다.',
    '5) 인라인 <style>만 사용. 외부 리소스·이미지·폰트 없음. 필요한 시각 표현은 모두 CSS로.',
    '',
    '제품 문서(참고):',
    '"""',
    productDoc,
    '"""',
    '',
    '설명 문장이나 코드펜스(```) 없이 완전한 HTML 문서 하나만 출력하라.',
  ].join('\n');
}
